{
  "private": true,
  "name": "monorepo-astro-payload",
  "version": "1.0.0",
  "scripts": {
    "install": "pnpm install",
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "dev:frontend": "pnpm --filter frontend run dev",
    "dev:backend": "pnpm --filter backend run dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
