---
import 'open-props/style'
---

<style is:global>

    * {
        box-sizing: border-box;
        max-width: 100% ;
    }

    html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        scroll-behavior: smooth;
        background-color: var(--gray-2);
        color: var(--gray-9);
        font-family: var(--font-sans), serif;
        overflow-x: hidden;
    }


    .page-container {
        display: flex;
        flex-direction: column;
        min-height: 100vh; /* Fait en sorte que le footer reste en bas */
    }

    .content {
        flex: 1; /* Permet au contenu de grandir et de pousser le footer en bas */
    }


    .container {
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 var(--size-4);
    }



    .footer {
        position: relative; /* 🔹 Empêche qu'il soit en `absolute` et chevauche le contenu */
        width: 100%;
        background-color: var(--gray-4);
        color: var(--gray-9);
        text-align: center;
        padding: var(--size-4);
    }



    /* Bandeau */
    .banner {
        position: relative;
        width: 100%;
        height: var(--size-12);
        overflow: hidden;
    }

    .home-banner {
        height: 100vh;
        width: 100%;
    }

    .banner img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .banner .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-wrap: wrap;
        flex-direction: column;
        color: var(--gray-9);
    }

    .overlay-other {
        display: none;
    }

    .banner h1 {
        color: white;
        font-size: var(--font-size-6);
    }

    .scroll-button {
        position: absolute;
        bottom: 50px; /* Ajuste selon ton design */
        left: 50%;
        transform: translateX(-50%);
        padding: var(--size-3) var(--size-5);
        font-size: var(--font-size-3);
        background: var(--blue-6);
        color: white;
        border: none;
        border-radius: var(--radius-2);
        cursor: pointer;
        transition: background 0.3s ease, transform 0.3s ease;
    }

    .scroll-button:hover {
        background: var(--blue-8);
        transform: translateX(-50%) scale(1.05);
    }


    /* Navigation */
    .navbar {
        display: flex;
        position: absolute;
        background: transparent !important; /* 🔹 Supprime le fond */
        justify-content: flex-start;
        padding: var(--size-2) var(--size-4);
        z-index: 10;
    }


    .navbar ul {
        list-style: none;
        display: flex;
        gap: var(--size-4);
        padding: 0 var(--size-4);
    }

    .navbar li {
        margin: 0;
    }


    .navbar a {
        position: relative;
        text-decoration: none;
        font-size: 18px;
        font-weight: bold;
        padding: 10px 15px;
        display: inline-block;
        color: var(--gray-1);
    }

    .home-navbar a {
        color: var(--gray-9);
    }


    .navbar a:hover {
        color: var(--gray-7);
    }

    .navbar a::before,
    .navbar a::after {
        content: "";
        position: absolute;
        left: 0;
        width: 100%;
        height: 2px; /* Épaisseur de la barre */
        background-color: black; /* Couleur de la barre */
        transform: scaleX(0); /* Cache la barre au départ */
        transition: transform 0.3s ease-in-out;
    }

    .navbar a::before {
        top: 0; /* Barre en haut */
        transform-origin: left;
    }

    .navbar a::after {
        bottom: 0; /* Barre en bas */
        transform-origin: left;
    }

    /* Effet lors du hover */
    .navbar a:hover::before,
    .navbar a:hover::after {
        transform: scaleX(1); /* Affichage progressif de la barre */
    }

    /* Contenu */
    main {
        padding: var(--size-5);
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        row-gap: var(--size-6);
    }

    .logo {
        height: var(--size-13);
    }

    .menu-logo {
        height: var(--size-7);
        padding: var(--size-2);
    }


    /* Formulaire de contact */

    .form-container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        margin-bottom: var(--size-6) ;
        padding: var(--size-5);

    }

    .contact-form {
        background: var(--gray-0);
        padding: var(--size-5);
        border-radius: var(--radius-2);
        box-shadow: var(--shadow-3);
        max-width: 400px;
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: var(--size-3);
    }

    .contact-form label {
        font-weight: bold;
        font-size: var(--font-size-3);
    }

    .contact-form input,
    .contact-form textarea {
        padding: var(--size-2);
        border: 1px solid var(--gray-5);
        border-radius: var(--radius-2);
        font-size: var(--font-size-3);
    }

    .contact-form button {
        background: var(--blue-6);
        color: white;
        padding: var(--size-3);
        font-size: var(--font-size-4);
        border: none;
        border-radius: var(--radius-2);
        cursor: pointer;
        transition: background 0.3s ease;
    }

    .contact-form button:hover {
        background: var(--blue-8);
    }

    /* ARTICLES */
    .articles-list {
        list-style: none;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: var(--size-6); /* 🔹 Espacement entre les cartes */
    }

    .article-card {
        background: var(--gray-0);
        border-radius: var(--radius-3);
        box-shadow: var(--shadow-4);
        transition: transform 0.3s ease-in-out;
        overflow: hidden;
    }

    .article-card:hover {
        transform: scale(1.02);
    }

    .article-link {
        display: flex; /* 🔹 Transforme <a> en bloc flex */
        text-decoration: none;
        color: inherit;
        width: 100%;
        height: 100%;
    }

    .article-image {
        width: 150px;
        height: 100px;
        object-fit: cover;
        border-top-left-radius: var(--radius-3);
        border-bottom-left-radius: var(--radius-3);
    }

    .article-content {
        padding: var(--size-4);
        flex: 1;
    }

    article {
        padding: var(--size-5);
        text-align: left;

    }

    .spinner {
        border: 4px solid rgba(0, 0, 0, 0.1);
        border-left-color: #000;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        animation: spin 1s linear infinite;
        display: inline-block;
        margin-right: 8px;
    }

    @keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }

    .success-check {
        color: green;
        font-size: 20px;
        font-weight: bold;
        display: inline-block;
        margin-right: 8px;
        transition: opacity 0.5s ease-in-out;
    }


</style>